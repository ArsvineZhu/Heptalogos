{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "evidence",
  "projectType": "library",
  "sourceRoot": "packages/evidence/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint packages/evidence"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root packages/evidence"
      }
    }
  }
}
