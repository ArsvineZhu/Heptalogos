{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "evidence",
  "projectType": "library",
  "tags": ["kind:product", "area:execution"],
  "sourceRoot": "packages/evidence/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "oxlint --type-aware packages/evidence"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root packages/evidence"
      }
    }
  }
}
