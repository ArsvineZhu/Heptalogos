{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "persistence",
  "projectType": "library",
  "tags": ["kind:product", "area:data"],
  "sourceRoot": "packages/persistence/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "oxlint --type-aware packages/persistence"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root packages/persistence --exclude **/*.integration.test.ts"
      }
    },
    "test:integration": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root packages/persistence test/integration/persistence.integration.test.ts"
      }
    }
  }
}
