{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "signal",
  "projectType": "library",
  "tags": ["kind:product", "area:service"],
  "sourceRoot": "packages/signal/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint packages/signal"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root packages/signal"
      }
    }
  }
}
