{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "time-service",
  "projectType": "library",
  "tags": ["kind:product", "area:execution"],
  "sourceRoot": "packages/time-service/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint packages/time-service"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root packages/time-service"
      }
    }
  }
}
