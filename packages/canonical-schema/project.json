{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "canonical-schema",
  "projectType": "library",
  "tags": ["kind:product", "area:data"],
  "sourceRoot": "packages/canonical-schema/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "oxlint --type-aware packages/canonical-schema"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root packages/canonical-schema --exclude **/*.integration.test.ts"
      }
    }
  }
}
