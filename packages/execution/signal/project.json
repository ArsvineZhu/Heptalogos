{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "signal",
  "projectType": "library",
  "tags": ["kind:product", "area:service"],
  "sourceRoot": "packages/execution/signal/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "oxlint --type-aware packages/execution/signal"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root packages/execution/signal"
      }
    }
  }
}
