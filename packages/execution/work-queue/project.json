{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "work-queue",
  "projectType": "library",
  "tags": ["kind:product", "area:work-queue"],
  "sourceRoot": "packages/execution/work-queue/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "oxlint --type-aware packages/execution/work-queue"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root packages/execution/work-queue"
      }
    }
  }
}
