{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "host-ownership",
  "projectType": "library",
  "tags": ["kind:product", "area:bootstrap"],
  "sourceRoot": "packages/bootstrap/host-ownership/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "oxlint --type-aware packages/bootstrap/host-ownership"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root packages/bootstrap/host-ownership --exclude **/*.integration.test.ts"
      }
    },
    "test:integration": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root packages/bootstrap/host-ownership test/integration/host-ownership.integration.test.ts"
      }
    }
  }
}
