{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "private-postgres",
  "projectType": "library",
  "tags": ["kind:product", "area:bootstrap"],
  "sourceRoot": "packages/bootstrap/private-postgres/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "oxlint --type-aware packages/bootstrap/private-postgres"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root packages/bootstrap/private-postgres --exclude **/*.integration.test.ts"
      }
    },
    "test:integration": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root packages/bootstrap/private-postgres test/integration/controller.integration.test.ts"
      }
    }
  }
}
