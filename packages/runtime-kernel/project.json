{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "runtime-kernel",
  "projectType": "library",
  "tags": ["kind:product", "area:runtime"],
  "sourceRoot": "packages/runtime-kernel/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "eslint packages/runtime-kernel" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root packages/runtime-kernel"
      }
    }
  }
}
