{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "foundation-contracts",
  "projectType": "library",
  "tags": ["kind:product", "area:shared"],
  "sourceRoot": "packages/foundation-contracts/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "oxlint --type-aware packages/foundation-contracts"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root packages/foundation-contracts"
      }
    }
  }
}
