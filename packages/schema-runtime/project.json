{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "schema-runtime",
  "projectType": "library",
  "sourceRoot": "packages/schema-runtime/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint packages/schema-runtime"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root packages/schema-runtime"
      }
    }
  }
}
