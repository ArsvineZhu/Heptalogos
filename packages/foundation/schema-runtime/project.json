{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "schema-runtime",
  "projectType": "library",
  "tags": ["kind:product", "area:shared"],
  "sourceRoot": "packages/foundation/schema-runtime/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "oxlint --type-aware packages/foundation/schema-runtime"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root packages/foundation/schema-runtime"
      }
    }
  }
}
