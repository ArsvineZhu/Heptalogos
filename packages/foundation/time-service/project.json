{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "time-service",
  "projectType": "library",
  "tags": ["kind:product", "area:execution"],
  "sourceRoot": "packages/foundation/time-service/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "oxlint --type-aware packages/foundation/time-service"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root packages/foundation/time-service"
      }
    }
  }
}
