{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "subject",
  "projectType": "library",
  "tags": ["kind:product", "area:subject"],
  "sourceRoot": "packages/product/subject/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "oxlint --type-aware packages/product/subject"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root packages/product/subject"
      }
    }
  }
}
