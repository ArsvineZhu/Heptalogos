{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "messaging",
  "projectType": "library",
  "tags": ["kind:product", "area:messaging"],
  "sourceRoot": "packages/product/messaging/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "oxlint --type-aware packages/product/messaging"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root packages/product/messaging"
      }
    }
  }
}
