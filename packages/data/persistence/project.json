{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "persistence",
  "projectType": "library",
  "tags": ["kind:product", "area:data"],
  "sourceRoot": "packages/data/persistence/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "oxlint --type-aware packages/data/persistence"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root packages/data/persistence --exclude **/*.integration.test.ts"
      }
    },
    "test:integration": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root packages/data/persistence test/integration/persistence.integration.test.ts"
      }
    }
  }
}
