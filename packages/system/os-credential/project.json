{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "os-credential",
  "projectType": "library",
  "tags": ["kind:product", "area:shared"],
  "sourceRoot": "packages/system/os-credential/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "oxlint --type-aware packages/system/os-credential" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root packages/system/os-credential"
      }
    }
  }
}
