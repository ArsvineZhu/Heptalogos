{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "network-access",
  "projectType": "library",
  "tags": ["kind:product", "area:service"],
  "sourceRoot": "packages/system/network-access/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "oxlint --type-aware packages/system/network-access" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root packages/system/network-access"
      }
    }
  }
}
