{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "configuration",
  "projectType": "library",
  "tags": ["kind:product", "area:service"],
  "sourceRoot": "packages/system/configuration/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "oxlint --type-aware packages/system/configuration" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root packages/system/configuration"
      }
    }
  }
}
