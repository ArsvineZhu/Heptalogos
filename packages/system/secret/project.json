{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "secret",
  "projectType": "library",
  "tags": ["kind:product", "area:service"],
  "sourceRoot": "packages/system/secret/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "oxlint --type-aware packages/system/secret" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root packages/system/secret"
      }
    }
  }
}
